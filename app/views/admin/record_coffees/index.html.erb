<div class="container w-75">
  <div class="background-board py-3 px-5 my-4">
    <%= render 'admin/searches/search' %>
  </div>
  <div class="background-board py-3 px-5 my-4">
  <div class="margin-y">
    <div class="d-flex align-items-center justify-content-between">
      <h4 class="h4-review mb-4">Coffee Review投稿一覧</h4>
    </div>
    <%= link_to "Coffee", admin_record_coffees_path %> | <%= link_to "Tea", admin_record_tea_leaves_path %>
      <%= render 'admin/record_coffees/index', record_coffee: @record_coffee %>
  </div>
  <%= paginate @record_coffees %>
</div>
